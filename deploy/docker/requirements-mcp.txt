# MCP server Python dependencies
# These match what goldilocks-mcp needs without modifying the base goldilocks package

# MCP protocol
mcp>=1.0.0

# Science stack (used by goldilocks for inference)
pymatgen>=2024.11.13
ase>=3.25.0
numpy>=1.26.0
scipy>=1.12.0
joblib>=1.3.0

# ML models
torch>=2.7.0
alignn>=2024.5.27

# HTTP/SSH for HPC interaction
paramiko>=3.4.0
requests>=2.31.0
